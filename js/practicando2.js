let nombre = "Lily";
let edad = 18;
let mayorEdad = edad >= 18 ? true : false;

/* acá abajo este mismo código lo hicimos 
en una sola línea (donde dice mayorEdad)
estamos haciendo un operador Ternario
 acá arriba
 
 el  operador ternario lleva solamente
 un if y un else */
if (edad >= 18) {
    mayorEdad = true ;
} else {
    mayorEdad = false;
}